[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "audio-listener"
version = "0.1.0"
requires-python = ">=3.9"
description = "Convert MIDI/audio files to MusicalAnalysis symbolic IR"
